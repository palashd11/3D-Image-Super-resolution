import tensorflow as tf

def pooling(value, ksize):
	stride
	padding
	
	#tensorflow : Performs the max pooling on the input.	
	return tf.nn.max_pool(value, ksize, strides, padding, data_format='NHWC', name=None)
